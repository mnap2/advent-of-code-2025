import { Lobby } from "../solutions/Day3";

describe("lobby tests", () => {
    describe("part one", () => {
        it('should return actual solution to the puzzle', () => {
            expect(Lobby()).toBe(17193);
        });
    })

    describe("part two", () => {

    })
});